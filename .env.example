# ─── Base de datos ────────────────────────────────────────────────
POSTGRES_USER=yaku
POSTGRES_PASSWORD=yaku_dev_2026
POSTGRES_DB=yakuni
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg://yaku:yaku_dev_2026@db:5432/yakuni

# ─── Redis (cola de tareas / recordatorios) ───────────────────────
REDIS_URL=redis://redis:6379/0

# ─── Seguridad / JWT ──────────────────────────────────────────────
JWT_SECRET=cambia-esto-en-produccion-con-una-clave-larga-y-aleatoria
JWT_ALG=HS256
JWT_EXP_MIN=720

# ─── Mensajería (Twilio) — dejar en blanco para MODO SIMULADO ─────
# En el MVP/demo, si estas variables están vacías los mensajes se
# registran en la BD y en consola sin salir realmente (SMS_MODO=simulado).
SMS_MODO=simulado
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
WHATSAPP_FROM=

# ─── App ──────────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
SEED_DEMO=true
